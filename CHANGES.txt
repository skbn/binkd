Errors related to forks and threads have been corrected and header errors in includes, and some missing "extern"
binkd.c
binlog.c
client.c
client.h
exitproc.c
ftnnode.c
ftnq.c
iptools.h
Makefile.amiga
protocol.c
protocol.h
readcfg.c
README.md
rfc2553.h
server.h
tools.c

rfc2553.c	Add variable "int h_errno = 0;"
run.c		Added code to execute in an external program from the shell
sem.h		Add struct EVENTSEM and the declarations of the functions for the missing threads (_InitEventSem, _CleanEventSem, _PostSem, _WaitSem)

server.c	Add extern EVENTSEM and "#if defined(OS2) || defined(AMIGA)", This fixes a bug that caused the socket to close without releasing memory and not recreating it, resulting in a signal #15 error.

amiga:
rename.c	Fix with rename, which was causing a signal #15 with ixemul causing it to close sockets in server.c
sem.c		_InitEventSem, _CleanEventSem, _PostSem, _WaitSem. With these functions added, the remaining ones needed to complete the threading section were also included.

LIBS:
They come with ADE
ixemul.library
ixnet.library
