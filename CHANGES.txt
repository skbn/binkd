Errors related to forks and threads have been corrected and header errors in includes, and some missing "extern"
binkd.c
binlog.c
client.c
client.h
exitproc.c
ftnnode.c
ftnq.c
iptools.h
Makefile.amiga
protocol.c
protocol.h
README.md
rfc2553.h
server.h
tools.c

readcfg.c	Fix error with -C option and random crashes
branch.c	Fix some bug with atexit
inbound.c	Skip mtime para arcmail/pkt/tic/req. By preventing it from requesting the files multiple times, and by not allowing it to send an M_GOT to the server

rfc2553.c	Add variable "int h_errno = 0;" warning bind -- getaddrinfo
run.c		Added code to execute in an external program from the shell and Fix some bugs with console
sem.h		Add struct EVENTSEM and the declarations of the functions for the missing threads (_InitEventSem, _CleanEventSem, _PostSem, _WaitSem)

server.c	Add extern EVENTSEM and "#if defined(OS2) || defined(AMIGA)", This fixes a bug that caused the socket to close without releasing memory and not recreating it, resulting in a signal #15 error. bind() error and random crash. ermission denied (EACCES/EPERM)

amiga:
rename.c	Fix with rename, which was causing a signal #15 with ixemul causing it to close sockets in server.c, delete destination before renaming

sem.c		_InitEventSem, _CleanEventSem, _PostSem, _WaitSem. With these functions added, the remaining ones needed to complete the threading section were also included.

LIBS:
They come with ADE
ixemul.library
ixnet.library
